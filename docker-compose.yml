version: '3.8'

services:
  hookflow:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - ConnectionStrings__DefaultConnection=Data Source=/app/data/hookflow.db
      - ASPNETCORE_ENVIRONMENT=Production
    restart: unless-stopped
